/**
 * Central icon set — re-exported from lucide-react so the whole app shares
 * one crisp, consistent line-icon family. Existing call sites keep their
 * names (SearchIcon, CartIcon, …) and props (className, width/height).
 */
export {
  Search as SearchIcon,
  ShoppingCart as CartIcon,
  Package as PackageIcon,
  Check as CheckIcon,
  ArrowRight as ArrowRightIcon,
  ShieldCheck as ShieldIcon,
  Truck as TruckIcon,
  Tag as TagIcon,
  AlertTriangle as AlertIcon,
  Trash2 as TrashIcon,
} from "lucide-react";
